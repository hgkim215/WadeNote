# WadeNote v1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 민감한 개인정보를 유형별 템플릿으로 구조화 저장하고, Face ID로 잠그며, iCloud(CloudKit)로 모든 기기에 자동 복원하는 iOS 네이티브 앱 v1.

**Architecture:** SwiftUI + SwiftData(CloudKit private DB 자동 동기화). 로직은 책임별 모듈(Models / Templates / Store / AttachmentStore / Clipboard / AppLock)로 분리하고 단위 테스트. UI는 잠금/홈/상세/추가편집 4화면. 사진 첨부만 CryptoKit으로 추가 암호화(키는 Keychain), 텍스트는 CloudKit+ADP에 의존.

**Tech Stack:** Swift 6, SwiftUI, SwiftData, CloudKit, CryptoKit, LocalAuthentication, Security(Keychain), Swift Testing(`import Testing`), Xcode 16+.

## Global Constraints

- 최소 배포 타깃: **iOS 18.0** (SwiftData CloudKit 통합·Swift Testing 안정 버전).
- 언어/동시성: **Swift 6**, strict concurrency. `@MainActor` UI, 모델 액세스는 메인 컨텍스트 기준.
- 번들 식별자: `com.wadenote.app` (CloudKit 컨테이너 `iCloud.com.wadenote.app`).
- 앱 이름 표기: 항상 **WadeNote** (한 단어, 카멜케이스).
- 유형은 정확히 4종: 로그인 / 카드·은행 / 신분증 / 보안 메모. + 사용자 정의 필드.
- 유형 컬러(그라데이션 밝은→진한 / 단색 액센트):
  - 로그인 `#4F8BFF→#2D5BFF` / `#2D5BFF`
  - 카드·은행 `#34D27B→#13A958` / `#1FB866`
  - 신분증 `#B26BF7→#8741E6` / `#8741E6`
  - 보안 메모 `#AAB0BD→#7C828F` / `#7C828F`
- 라이트 배경 `#f4f2ee`, 카드 `#ffffff`, 주요 텍스트 `#15151a`, 보조 `#8a8a92`, 액션 블루 `#2D5BFF`, 즐겨찾기 별 `#FFB300`.
- 비밀값 필드는 모노스페이스 표시 + 기본 마스킹(••••).
- 빠른 복사 후 클립보드 자동삭제 기본값: **30초**.
- 자동 재잠금 기본값: 백그라운드 진입 즉시 잠금 + 포그라운드 복귀 시 Face ID.
- 참조 문서: `docs/superpowers/specs/2026-06-22-wadenote-design.md`, `...-design-brief.md`, `...-visual-design.md`.

---

## File Structure

```
WadeNote.xcodeproj
WadeNote/
  App/
    WadeNoteApp.swift            # @main, ModelContainer(CloudKit), 루트 게이트
    RootView.swift               # AppLock 상태에 따라 LockView/HomeView 전환
  Models/
    ItemType.swift               # enum: login/card/identity/memo
    Field.swift                  # @Model: 라벨·값·필드타입·정렬순서
    Item.swift                   # @Model: 제목·유형·즐겨찾기·필드들·첨부·날짜
  Templates/
    FieldKind.swift              # enum: text/secret/url/date
    Template.swift               # 유형별 기본 필드 정의(데이터)
    ItemType+Theme.swift         # 유형별 색·SF Symbol 토큰
  Store/
    ItemStore.swift              # CRUD·검색·즐겨찾기 (ModelContext 래퍼)
  Security/
    AttachmentStore.swift        # 사진 암호화 저장/복호화(CryptoKit+Keychain)
    KeychainKey.swift            # 대칭키 생성/조회(Keychain)
    AppLock.swift                # @Observable: Face ID·잠금 상태
  Clipboard/
    SecureClipboard.swift        # 복사 + N초 자동삭제
  DesignSystem/
    Colors.swift                 # Color 토큰
    TypeTile.swift               # 그라데이션 아이콘 타일 뷰
    FieldRow.swift               # 필드 행 뷰(라벨·값·눈·복사)
    Toast.swift                  # 복사 토스트
  Screens/
    LockView.swift
    HomeView.swift
    ItemDetailView.swift
    ItemEditView.swift
  Assets.xcassets/               # AppIcon (라이트/다크 적응형), 색상
WadeNoteTests/
  ItemTests.swift
  TemplateTests.swift
  ItemStoreTests.swift
  AttachmentStoreTests.swift
  SecureClipboardTests.swift
```

---

## Task 1: Xcode 프로젝트 스캐폴드 + SwiftData/CloudKit 구성

**Files:**
- Create: `WadeNote.xcodeproj`, `WadeNote/App/WadeNoteApp.swift`, `WadeNote/App/RootView.swift`
- Create: `WadeNoteTests/SmokeTests.swift`

**Interfaces:**
- Produces: `WadeNoteApp`(@main), 앱 전역 `ModelContainer` (CloudKit private DB), 빈 `RootView`.

- [ ] **Step 1: 프로젝트 생성**

```bash
# Xcode > New Project > iOS App, Interface: SwiftUI, Storage: SwiftData, Testing: Swift Testing
# Product Name: WadeNote, Bundle ID: com.wadenote.app, Min iOS 18.0
# 또는 기존 도구로 생성 후 아래 설정 확인.
```

수동 설정(프로젝트 Signing & Capabilities):
- iCloud 추가 → CloudKit 체크 → 컨테이너 `iCloud.com.wadenote.app` 생성.
- Background Modes → Remote notifications 체크(CloudKit 동기화 푸시).
- Capabilities에 **Face ID 사용 설명**: Info.plist `NSFaceIDUsageDescription` = "WadeNote 잠금 해제에 Face ID를 사용합니다."

- [ ] **Step 2: 실패하는 스모크 테스트 작성**

`WadeNoteTests/SmokeTests.swift`:
```swift
import Testing
@testable import WadeNote

@Test func appContainerCreatesInMemory() throws {
    let container = try WadeNoteApp.makeContainer(inMemory: true)
    #expect(container.mainContext.insertedModelsArray.isEmpty)
}
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `xcodebuild test -scheme WadeNote -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:WadeNoteTests/SmokeTests`
Expected: FAIL — `makeContainer` 미정의 컴파일 에러.

- [ ] **Step 4: 최소 구현**

`WadeNote/App/WadeNoteApp.swift`:
```swift
import SwiftUI
import SwiftData

@main
struct WadeNoteApp: App {
    let container: ModelContainer

    init() {
        container = try! Self.makeContainer(inMemory: false)
    }

    static func makeContainer(inMemory: Bool) throws -> ModelContainer {
        let config = ModelConfiguration(
            isStoredInMemoryOnly: inMemory,
            cloudKitDatabase: inMemory ? .none : .private("iCloud.com.wadenote.app")
        )
        return try ModelContainer(for: Item.self, Field.self, configurations: config)
    }

    var body: some Scene {
        WindowGroup { RootView() }
            .modelContainer(container)
    }
}
```

`WadeNote/App/RootView.swift`:
```swift
import SwiftUI

struct RootView: View {
    var body: some View { Text("WadeNote") }
}
```

> 주의: `Item`/`Field`는 Task 2에서 정의. 이 Task는 Task 2와 함께 컴파일되어야 하므로, Task 2의 모델 빈 껍데기를 먼저 두거나 Task 2 완료 후 본 테스트를 통과시킨다. 실행 순서상 Task 2를 먼저 머지해도 무방.

- [ ] **Step 5: 테스트 통과 확인 후 커밋**

Run: 위 동일 명령
Expected: PASS

```bash
git init && git add -A
git commit -m "chore: scaffold WadeNote app with SwiftData+CloudKit container"
```

---

## Task 2: 데이터 모델 (ItemType, Field, Item)

**Files:**
- Create: `WadeNote/Models/ItemType.swift`, `WadeNote/Models/Field.swift`, `WadeNote/Models/Item.swift`
- Test: `WadeNoteTests/ItemTests.swift`

**Interfaces:**
- Consumes: 없음.
- Produces:
  - `enum ItemType: String, Codable, CaseIterable { case login, card, identity, memo }`
  - `enum FieldKind: String, Codable { case text, secret, url, date }` (Task 3에서 사용하지만 여기서 정의)
  - `@Model final class Field { var label: String; var value: String; var kindRaw: String; var order: Int; var item: Item? }` + `var kind: FieldKind` 계산 프로퍼티
  - `@Model final class Item { var title: String; var typeRaw: String; var isFavorite: Bool; var createdAt: Date; var updatedAt: Date; var attachmentIDs: [String]; @Relationship(deleteRule: .cascade) var fields: [Field] }` + `var type: ItemType` 계산 프로퍼티

- [ ] **Step 1: 실패하는 테스트 작성**

`WadeNoteTests/ItemTests.swift`:
```swift
import Testing
import SwiftData
@testable import WadeNote

@MainActor
private func makeContext() throws -> ModelContext {
    let c = try WadeNoteApp.makeContainer(inMemory: true)
    return c.mainContext
}

@MainActor @Test func itemStoresTypeAndFields() throws {
    let ctx = try makeContext()
    let item = Item(title: "Netflix", type: .login)
    item.fields = [Field(label: "아이디", value: "wade@email.com", kind: .text, order: 0),
                   Field(label: "비밀번호", value: "pw123", kind: .secret, order: 1)]
    ctx.insert(item)
    try ctx.save()

    let fetched = try ctx.fetch(FetchDescriptor<Item>())
    #expect(fetched.count == 1)
    #expect(fetched[0].type == .login)
    #expect(fetched[0].fields.count == 2)
    #expect(fetched[0].fields.first(where: { $0.kind == .secret })?.value == "pw123")
}

@MainActor @Test func cascadeDeletesFields() throws {
    let ctx = try makeContext()
    let item = Item(title: "X", type: .memo)
    item.fields = [Field(label: "본문", value: "secret", kind: .text, order: 0)]
    ctx.insert(item); try ctx.save()
    ctx.delete(item); try ctx.save()
    #expect(try ctx.fetch(FetchDescriptor<Field>()).isEmpty)
}
```

- [ ] **Step 2: 실패 확인**

Run: `xcodebuild test -scheme WadeNote -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:WadeNoteTests/ItemTests`
Expected: FAIL — `Item`/`Field` 이니셜라이저 미정의.

- [ ] **Step 3: 구현**

`WadeNote/Models/ItemType.swift`:
```swift
enum ItemType: String, Codable, CaseIterable, Identifiable {
    case login, card, identity, memo
    var id: String { rawValue }
    var displayName: String {
        switch self {
        case .login: "로그인"; case .card: "카드·은행"
        case .identity: "신분증"; case .memo: "보안 메모"
        }
    }
}

enum FieldKind: String, Codable { case text, secret, url, date }
```

`WadeNote/Models/Field.swift`:
```swift
import SwiftData

@Model final class Field {
    var label: String = ""
    var value: String = ""
    var kindRaw: String = FieldKind.text.rawValue
    var order: Int = 0
    var item: Item?

    var kind: FieldKind {
        get { FieldKind(rawValue: kindRaw) ?? .text }
        set { kindRaw = newValue.rawValue }
    }

    init(label: String, value: String, kind: FieldKind, order: Int) {
        self.label = label; self.value = value
        self.kindRaw = kind.rawValue; self.order = order
    }
}
```

`WadeNote/Models/Item.swift`:
```swift
import SwiftData
import Foundation

@Model final class Item {
    var title: String = ""
    var typeRaw: String = ItemType.login.rawValue
    var isFavorite: Bool = false
    var createdAt: Date = Date()
    var updatedAt: Date = Date()
    var attachmentIDs: [String] = []
    @Relationship(deleteRule: .cascade, inverse: \Field.item)
    var fields: [Field] = []

    var type: ItemType {
        get { ItemType(rawValue: typeRaw) ?? .login }
        set { typeRaw = newValue.rawValue }
    }

    init(title: String, type: ItemType) {
        self.title = title; self.typeRaw = type.rawValue
        self.createdAt = Date(); self.updatedAt = Date()
    }
}
```

> CloudKit 통합 요건: 모든 저장 프로퍼티는 기본값 보유, 관계는 옵셔널/기본 빈 배열, `@Attribute(.unique)` 미사용. 위 모델은 이를 준수.

- [ ] **Step 4: 통과 확인**

Run: 위 동일
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add WadeNote/Models WadeNoteTests/ItemTests.swift
git commit -m "feat: add Item/Field/ItemType SwiftData models (CloudKit-safe)"
```

---

## Task 3: 템플릿 + 유형 테마 (Templates)

**Files:**
- Create: `WadeNote/Templates/Template.swift`, `WadeNote/Templates/ItemType+Theme.swift`
- Test: `WadeNoteTests/TemplateTests.swift`

**Interfaces:**
- Consumes: `ItemType`, `FieldKind`, `Field` (Task 2).
- Produces:
  - `struct FieldSpec { let label: String; let kind: FieldKind }`
  - `enum Template { static func fields(for type: ItemType) -> [FieldSpec]; static func makeFields(for type: ItemType) -> [Field] }`
  - `extension ItemType { var accentHex: String; var gradientHex: (String, String); var symbolName: String }`

- [ ] **Step 1: 실패하는 테스트**

`WadeNoteTests/TemplateTests.swift`:
```swift
import Testing
@testable import WadeNote

@Test func loginTemplateHasExpectedFields() {
    let labels = Template.fields(for: .login).map(\.label)
    #expect(labels == ["서비스명", "아이디", "비밀번호", "URL", "메모"])
    let pw = Template.fields(for: .login).first { $0.label == "비밀번호" }
    #expect(pw?.kind == .secret)
}

@Test func cardTemplateMarksSecrets() {
    let specs = Template.fields(for: .card)
    #expect(specs.first { $0.label == "CVC" }?.kind == .secret)
    #expect(specs.map(\.label).contains("유효기간"))
}

@Test func makeFieldsAssignsOrder() {
    let fields = Template.makeFields(for: .identity)
    #expect(fields.map(\.order) == Array(0..<fields.count))
}

@Test func everyTypeHasTheme() {
    for t in ItemType.allCases {
        #expect(!t.accentHex.isEmpty)
        #expect(!t.symbolName.isEmpty)
    }
    #expect(ItemType.login.accentHex == "2D5BFF")
}
```

- [ ] **Step 2: 실패 확인**

Run: `xcodebuild test -scheme WadeNote -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:WadeNoteTests/TemplateTests`
Expected: FAIL — `Template`/테마 미정의.

- [ ] **Step 3: 구현**

`WadeNote/Templates/Template.swift`:
```swift
struct FieldSpec { let label: String; let kind: FieldKind }

enum Template {
    static func fields(for type: ItemType) -> [FieldSpec] {
        switch type {
        case .login:
            [.init(label: "서비스명", kind: .text), .init(label: "아이디", kind: .text),
             .init(label: "비밀번호", kind: .secret), .init(label: "URL", kind: .url),
             .init(label: "메모", kind: .text)]
        case .card:
            [.init(label: "카드/계좌명", kind: .text), .init(label: "번호", kind: .secret),
             .init(label: "유효기간", kind: .text), .init(label: "CVC", kind: .secret),
             .init(label: "비밀번호", kind: .secret), .init(label: "메모", kind: .text)]
        case .identity:
            [.init(label: "종류", kind: .text), .init(label: "번호", kind: .secret),
             .init(label: "발급일", kind: .date), .init(label: "만료일", kind: .date),
             .init(label: "메모", kind: .text)]
        case .memo:
            [.init(label: "제목", kind: .text), .init(label: "본문", kind: .text)]
        }
    }

    static func makeFields(for type: ItemType) -> [Field] {
        fields(for: type).enumerated().map { i, spec in
            Field(label: spec.label, value: "", kind: spec.kind, order: i)
        }
    }
}
```

`WadeNote/Templates/ItemType+Theme.swift`:
```swift
extension ItemType {
    var accentHex: String {
        switch self {
        case .login: "2D5BFF"; case .card: "1FB866"
        case .identity: "8741E6"; case .memo: "7C828F"
        }
    }
    var gradientHex: (String, String) {
        switch self {
        case .login: ("4F8BFF", "2D5BFF"); case .card: ("34D27B", "13A958")
        case .identity: ("B26BF7", "8741E6"); case .memo: ("AAB0BD", "7C828F")
        }
    }
    var symbolName: String {
        switch self {
        case .login: "lock.fill"; case .card: "creditcard.fill"
        case .identity: "person.text.rectangle.fill"; case .memo: "doc.text.fill"
        }
    }
}
```

- [ ] **Step 4: 통과 확인**

Run: 위 동일
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add WadeNote/Templates WadeNoteTests/TemplateTests.swift
git commit -m "feat: add per-type templates and color/symbol theme tokens"
```

---

## Task 4: ItemStore (CRUD · 검색 · 즐겨찾기)

**Files:**
- Create: `WadeNote/Store/ItemStore.swift`
- Test: `WadeNoteTests/ItemStoreTests.swift`

**Interfaces:**
- Consumes: `Item`, `Field`, `ItemType`, `Template` (Task 2–3).
- Produces (`@MainActor final class ItemStore`):
  - `init(context: ModelContext)`
  - `func create(type: ItemType, title: String) -> Item` (템플릿 필드 자동 생성, insert+save)
  - `func save() throws`
  - `func delete(_ item: Item) throws`
  - `func toggleFavorite(_ item: Item) throws`
  - `func all() throws -> [Item]` (updatedAt 내림차순)
  - `func search(_ query: String) throws -> [Item]` (제목·유형명·필드 라벨 대상, **필드 값 제외**)
  - `func favorites() throws -> [Item]`

- [ ] **Step 1: 실패하는 테스트**

`WadeNoteTests/ItemStoreTests.swift`:
```swift
import Testing
import SwiftData
@testable import WadeNote

@MainActor
private func makeStore() throws -> ItemStore {
    let ctx = try WadeNoteApp.makeContainer(inMemory: true).mainContext
    return ItemStore(context: ctx)
}

@MainActor @Test func createUsesTemplateFields() throws {
    let store = try makeStore()
    let item = store.create(type: .login, title: "Netflix")
    #expect(item.fields.map(\.label) == ["서비스명", "아이디", "비밀번호", "URL", "메모"])
    #expect(try store.all().count == 1)
}

@MainActor @Test func searchMatchesTitleAndLabelButNotValue() throws {
    let store = try makeStore()
    let item = store.create(type: .login, title: "Netflix")
    item.fields.first { $0.label == "비밀번호" }?.value = "topsecret"
    try store.save()

    #expect(try store.search("netf").count == 1)        // 제목
    #expect(try store.search("로그인").count == 1)       // 유형명
    #expect(try store.search("비밀번호").count == 1)     // 필드 라벨
    #expect(try store.search("topsecret").isEmpty)       // 필드 값 제외(보안)
}

@MainActor @Test func toggleFavoriteAndList() throws {
    let store = try makeStore()
    let item = store.create(type: .card, title: "신한카드")
    try store.toggleFavorite(item)
    #expect(item.isFavorite)
    #expect(try store.favorites().count == 1)
    try store.toggleFavorite(item)
    #expect(try store.favorites().isEmpty)
}

@MainActor @Test func deleteRemovesItem() throws {
    let store = try makeStore()
    let item = store.create(type: .memo, title: "메모")
    try store.delete(item)
    #expect(try store.all().isEmpty)
}
```

- [ ] **Step 2: 실패 확인**

Run: `xcodebuild test -scheme WadeNote -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:WadeNoteTests/ItemStoreTests`
Expected: FAIL — `ItemStore` 미정의.

- [ ] **Step 3: 구현**

`WadeNote/Store/ItemStore.swift`:
```swift
import SwiftData
import Foundation

@MainActor final class ItemStore {
    let context: ModelContext
    init(context: ModelContext) { self.context = context }

    func create(type: ItemType, title: String) -> Item {
        let item = Item(title: title, type: type)
        item.fields = Template.makeFields(for: type)
        for f in item.fields { f.item = item }
        context.insert(item)
        try? context.save()
        return item
    }

    func save() throws {
        // updatedAt 갱신은 호출측 책임이 아니라 여기서 일괄 처리하지 않음(편집 화면에서 명시 갱신)
        try context.save()
    }

    func delete(_ item: Item) throws { context.delete(item); try context.save() }

    func toggleFavorite(_ item: Item) throws {
        item.isFavorite.toggle(); item.updatedAt = Date(); try context.save()
    }

    func all() throws -> [Item] {
        try context.fetch(FetchDescriptor<Item>(
            sortBy: [SortDescriptor(\.updatedAt, order: .reverse)]))
    }

    func favorites() throws -> [Item] {
        try all().filter(\.isFavorite)
    }

    func search(_ query: String) throws -> [Item] {
        let q = query.trimmingCharacters(in: .whitespaces).lowercased()
        guard !q.isEmpty else { return try all() }
        return try all().filter { item in
            item.title.lowercased().contains(q)
            || item.type.displayName.lowercased().contains(q)
            || item.fields.contains { $0.label.lowercased().contains(q) }
        }
    }
}
```

> 검색은 인메모리 필터로 충분(개인 데이터 규모). 필드 *값*은 의도적으로 검색 대상에서 제외(보안).

- [ ] **Step 4: 통과 확인**

Run: 위 동일
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add WadeNote/Store WadeNoteTests/ItemStoreTests.swift
git commit -m "feat: add ItemStore with CRUD, label-only search, favorites"
```

---

## Task 5: Keychain 키 + AttachmentStore (사진 암호화)

**Files:**
- Create: `WadeNote/Security/KeychainKey.swift`, `WadeNote/Security/AttachmentStore.swift`
- Test: `WadeNoteTests/AttachmentStoreTests.swift`

**Interfaces:**
- Consumes: 없음(파일시스템 + Keychain).
- Produces:
  - `enum KeychainKey { static func loadOrCreateSymmetricKey(tag: String) throws -> SymmetricKey }`
  - `struct AttachmentStore { init(directory: URL, key: SymmetricKey); func save(_ data: Data) throws -> String; func load(id: String) throws -> Data; func delete(id: String) throws }`
  - `save`는 AES-GCM 암호문을 `directory/<uuid>.enc`로 쓰고 id(uuid) 반환.

- [ ] **Step 1: 실패하는 테스트**

`WadeNoteTests/AttachmentStoreTests.swift`:
```swift
import Testing
import CryptoKit
import Foundation
@testable import WadeNote

private func tempDir() -> URL {
    let url = FileManager.default.temporaryDirectory
        .appendingPathComponent(UUID().uuidString)
    try? FileManager.default.createDirectory(at: url, withIntermediateDirectories: true)
    return url
}

@Test func roundTripEncryptsAndDecrypts() throws {
    let key = SymmetricKey(size: .bits256)
    let store = AttachmentStore(directory: tempDir(), key: key)
    let plaintext = Data("신분증사진바이트".utf8)

    let id = try store.save(plaintext)
    #expect(try store.load(id: id) == plaintext)
}

@Test func storedBytesAreNotPlaintext() throws {
    let dir = tempDir()
    let key = SymmetricKey(size: .bits256)
    let store = AttachmentStore(directory: dir, key: key)
    let plaintext = Data("PLAINTEXT_MARKER".utf8)

    let id = try store.save(plaintext)
    let onDisk = try Data(contentsOf: dir.appendingPathComponent("\(id).enc"))
    #expect(!onDisk.contains(Data("PLAINTEXT_MARKER".utf8)))
}

@Test func deleteRemovesFile() throws {
    let dir = tempDir()
    let store = AttachmentStore(directory: dir, key: SymmetricKey(size: .bits256))
    let id = try store.save(Data([1,2,3]))
    try store.delete(id: id)
    #expect(!FileManager.default.fileExists(atPath: dir.appendingPathComponent("\(id).enc").path))
}
```

- [ ] **Step 2: 실패 확인**

Run: `xcodebuild test -scheme WadeNote -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:WadeNoteTests/AttachmentStoreTests`
Expected: FAIL — `AttachmentStore` 미정의.

- [ ] **Step 3: 구현**

`WadeNote/Security/AttachmentStore.swift`:
```swift
import CryptoKit
import Foundation

struct AttachmentStore {
    let directory: URL
    let key: SymmetricKey

    init(directory: URL, key: SymmetricKey) {
        self.directory = directory; self.key = key
        try? FileManager.default.createDirectory(at: directory, withIntermediateDirectories: true)
    }

    func save(_ data: Data) throws -> String {
        let sealed = try AES.GCM.seal(data, using: key)
        guard let combined = sealed.combined else {
            throw CocoaError(.coderInvalidValue)
        }
        let id = UUID().uuidString
        try combined.write(to: url(for: id), options: .completeFileProtection)
        return id
    }

    func load(id: String) throws -> Data {
        let combined = try Data(contentsOf: url(for: id))
        let box = try AES.GCM.SealedBox(combined: combined)
        return try AES.GCM.open(box, using: key)
    }

    func delete(id: String) throws {
        try FileManager.default.removeItem(at: url(for: id))
    }

    private func url(for id: String) -> URL {
        directory.appendingPathComponent("\(id).enc")
    }
}
```

`WadeNote/Security/KeychainKey.swift`:
```swift
import CryptoKit
import Security
import Foundation

enum KeychainKey {
    static func loadOrCreateSymmetricKey(tag: String) throws -> SymmetricKey {
        if let existing = try load(tag: tag) { return existing }
        let key = SymmetricKey(size: .bits256)
        try store(key, tag: tag)
        return key
    }

    private static func load(tag: String) throws -> SymmetricKey? {
        let query: [String: Any] = [
            kSecClass as String: kSecClassGenericPassword,
            kSecAttrAccount as String: tag,
            kSecReturnData as String: true
        ]
        var out: CFTypeRef?
        let status = SecItemCopyMatching(query as CFDictionary, &out)
        if status == errSecItemNotFound { return nil }
        guard status == errSecSuccess, let data = out as? Data else {
            throw NSError(domain: "Keychain", code: Int(status))
        }
        return SymmetricKey(data: data)
    }

    private static func store(_ key: SymmetricKey, tag: String) throws {
        let data = key.withUnsafeBytes { Data($0) }
        let query: [String: Any] = [
            kSecClass as String: kSecClassGenericPassword,
            kSecAttrAccount as String: tag,
            kSecValueData as String: data,
            kSecAttrAccessible as String: kSecAttrAccessibleWhenUnlockedThisDeviceOnly
        ]
        let status = SecItemAdd(query as CFDictionary, nil)
        guard status == errSecSuccess else { throw NSError(domain: "Keychain", code: Int(status)) }
    }
}
```

> `KeychainKey`는 시뮬레이터 Keychain 의존이라 단위 테스트는 `AttachmentStore`(주입된 키)만 검증. Keychain 경로는 Task 9 통합 수동 테스트로 확인.

- [ ] **Step 4: 통과 확인**

Run: 위 동일
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add WadeNote/Security WadeNoteTests/AttachmentStoreTests.swift
git commit -m "feat: add AES-GCM AttachmentStore and Keychain key provider"
```

---

## Task 6: SecureClipboard (복사 + 자동삭제)

**Files:**
- Create: `WadeNote/Clipboard/SecureClipboard.swift`
- Test: `WadeNoteTests/SecureClipboardTests.swift`

**Interfaces:**
- Consumes: 없음.
- Produces:
  - `protocol PasteboardProtocol { var string: String? { get set } }`
  - `@MainActor final class SecureClipboard { init(pasteboard: PasteboardProtocol, clearAfter: TimeInterval = 30); func copy(_ value: String); func clearNow() }`
  - 복사 후 `clearAfter`초 뒤 값이 그대로면 비움(다른 값이 복사돼 있으면 건드리지 않음).

- [ ] **Step 1: 실패하는 테스트**

`WadeNoteTests/SecureClipboardTests.swift`:
```swift
import Testing
import Foundation
@testable import WadeNote

final class FakePasteboard: PasteboardProtocol {
    var string: String?
}

@MainActor @Test func copySetsValueThenClears() async throws {
    let pb = FakePasteboard()
    let clip = SecureClipboard(pasteboard: pb, clearAfter: 0.1)
    clip.copy("pw123")
    #expect(pb.string == "pw123")
    try await Task.sleep(for: .milliseconds(200))
    #expect(pb.string == nil)
}

@MainActor @Test func doesNotClearIfReplaced() async throws {
    let pb = FakePasteboard()
    let clip = SecureClipboard(pasteboard: pb, clearAfter: 0.1)
    clip.copy("pw123")
    pb.string = "사용자가복사한다른값"
    try await Task.sleep(for: .milliseconds(200))
    #expect(pb.string == "사용자가복사한다른값")
}
```

- [ ] **Step 2: 실패 확인**

Run: `xcodebuild test -scheme WadeNote -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:WadeNoteTests/SecureClipboardTests`
Expected: FAIL — `SecureClipboard` 미정의.

- [ ] **Step 3: 구현**

`WadeNote/Clipboard/SecureClipboard.swift`:
```swift
import UIKit

protocol PasteboardProtocol: AnyObject { var string: String? { get set } }

extension UIPasteboard: PasteboardProtocol {}

@MainActor final class SecureClipboard {
    private let pasteboard: PasteboardProtocol
    private let clearAfter: TimeInterval
    private var task: Task<Void, Never>?

    init(pasteboard: PasteboardProtocol = UIPasteboard.general, clearAfter: TimeInterval = 30) {
        self.pasteboard = pasteboard; self.clearAfter = clearAfter
    }

    func copy(_ value: String) {
        pasteboard.string = value
        task?.cancel()
        task = Task { [weak self] in
            guard let self else { return }
            try? await Task.sleep(for: .seconds(clearAfter))
            if !Task.isCancelled, pasteboard.string == value { pasteboard.string = nil }
        }
    }

    func clearNow() { task?.cancel(); pasteboard.string = nil }
}
```

- [ ] **Step 4: 통과 확인**

Run: 위 동일
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add WadeNote/Clipboard WadeNoteTests/SecureClipboardTests.swift
git commit -m "feat: add SecureClipboard with auto-clear after timeout"
```

---

## Task 7: AppLock (Face ID · 잠금 상태)

**Files:**
- Create: `WadeNote/Security/AppLock.swift`
- Modify: `WadeNote/App/RootView.swift`

**Interfaces:**
- Consumes: 없음.
- Produces:
  - `protocol BiometricAuthenticating { func evaluate(reason: String) async -> Bool }`
  - `@MainActor @Observable final class AppLock { var isLocked: Bool; init(auth: BiometricAuthenticating = LAContextAuth()); func unlock() async; func lock() }`
  - `RootView`가 `isLocked`에 따라 `LockView`/`HomeView` 전환.

- [ ] **Step 1: 실패하는 테스트**

`WadeNoteTests/AppLockTests.swift`:
```swift
import Testing
@testable import WadeNote

struct StubAuth: BiometricAuthenticating {
    let success: Bool
    func evaluate(reason: String) async -> Bool { success }
}

@MainActor @Test func unlockSucceeds() async {
    let lock = AppLock(auth: StubAuth(success: true))
    #expect(lock.isLocked)
    await lock.unlock()
    #expect(!lock.isLocked)
}

@MainActor @Test func unlockFailsStaysLocked() async {
    let lock = AppLock(auth: StubAuth(success: false))
    await lock.unlock()
    #expect(lock.isLocked)
}

@MainActor @Test func lockReengages() async {
    let lock = AppLock(auth: StubAuth(success: true))
    await lock.unlock()
    lock.lock()
    #expect(lock.isLocked)
}
```

- [ ] **Step 2: 실패 확인**

Run: `xcodebuild test -scheme WadeNote -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:WadeNoteTests/AppLockTests`
Expected: FAIL — `AppLock` 미정의.

- [ ] **Step 3: 구현**

`WadeNote/Security/AppLock.swift`:
```swift
import LocalAuthentication

protocol BiometricAuthenticating { func evaluate(reason: String) async -> Bool }

struct LAContextAuth: BiometricAuthenticating {
    func evaluate(reason: String) async -> Bool {
        let ctx = LAContext()
        var error: NSError?
        guard ctx.canEvaluatePolicy(.deviceOwnerAuthentication, error: &error) else { return false }
        return await withCheckedContinuation { cont in
            ctx.evaluatePolicy(.deviceOwnerAuthentication, localizedReason: reason) { ok, _ in
                cont.resume(returning: ok)
            }
        }
    }
}

@MainActor @Observable final class AppLock {
    var isLocked = true
    private let auth: BiometricAuthenticating
    init(auth: BiometricAuthenticating = LAContextAuth()) { self.auth = auth }

    func unlock() async {
        let ok = await auth.evaluate(reason: "WadeNote 잠금 해제")
        if ok { isLocked = false }
    }
    func lock() { isLocked = true }
}
```

- [ ] **Step 4: 통과 확인**

Run: 위 동일
Expected: PASS

- [ ] **Step 5: RootView 잠금 게이트 연결 + scenePhase 재잠금**

`WadeNote/App/RootView.swift`:
```swift
import SwiftUI

struct RootView: View {
    @Environment(\.scenePhase) private var scenePhase
    @State private var lock = AppLock()

    var body: some View {
        Group {
            if lock.isLocked {
                LockView { await lock.unlock() }
            } else {
                HomeView()
            }
        }
        .onChange(of: scenePhase) { _, phase in
            if phase == .background { lock.lock() }
        }
    }
}
```

> `LockView`/`HomeView`는 Task 9·10에서 구현. 이 단계에서는 컴파일을 위해 두 화면의 최소 스텁이 존재해야 한다(Task 8에서 디자인 시스템과 함께 도입). 빌드 순서: Task 8 → 9 → 10 후 본 연결이 컴파일됨.

- [ ] **Step 6: 커밋**

```bash
git add WadeNote/Security/AppLock.swift WadeNote/App/RootView.swift WadeNoteTests/AppLockTests.swift
git commit -m "feat: add AppLock with Face ID and background re-lock"
```

---

## Task 8: 디자인 시스템 (색·타일·필드 행·토스트)

**Files:**
- Create: `WadeNote/DesignSystem/Colors.swift`, `WadeNote/DesignSystem/TypeTile.swift`, `WadeNote/DesignSystem/FieldRow.swift`, `WadeNote/DesignSystem/Toast.swift`
- Test: `WadeNoteTests/ColorsTests.swift`

**Interfaces:**
- Consumes: `ItemType` 테마(Task 3), `Field`/`FieldKind`(Task 2).
- Produces:
  - `extension Color { init(hex: String); static let appBackground, cardSurface, primaryText, secondaryText, actionBlue, favoriteStar }`
  - `extension ItemType { var gradient: LinearGradient; var accent: Color }`
  - `struct TypeTile: View`(유형·size) — 그라데이션 타일 + 흰 심볼.
  - `struct FieldRow: View`(field, onCopy, 내부 reveal 상태) — 라벨·값(비밀값 마스킹·모노)·눈 토글·복사 버튼.
  - `struct ToastModifier`/`.toast(_ message: Binding<String?>)` — 하단 페이드업 토스트.

- [ ] **Step 1: 실패하는 테스트(색 파서)**

`WadeNoteTests/ColorsTests.swift`:
```swift
import Testing
import SwiftUI
@testable import WadeNote

@Test func hexParsesToComponents() {
    let c = Color(hex: "2D5BFF").resolveRGB()
    #expect(abs(c.r - 45/255) < 0.01)
    #expect(abs(c.g - 91/255) < 0.01)
    #expect(abs(c.b - 255/255) < 0.01)
}

@Test func loginAccentMatchesToken() {
    #expect(ItemType.login.accent.resolveRGB() == Color(hex: "2D5BFF").resolveRGB())
}
```

`resolveRGB()` 테스트 헬퍼는 구현에 포함(아래 Colors.swift).

- [ ] **Step 2: 실패 확인**

Run: `xcodebuild test -scheme WadeNote -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:WadeNoteTests/ColorsTests`
Expected: FAIL — `Color(hex:)` 미정의.

- [ ] **Step 3: 구현 — Colors**

`WadeNote/DesignSystem/Colors.swift`:
```swift
import SwiftUI

extension Color {
    init(hex: String) {
        let s = hex.trimmingCharacters(in: CharacterSet(charactersIn: "#"))
        var v: UInt64 = 0; Scanner(string: s).scanHexInt64(&v)
        self.init(.sRGB,
                  red: Double((v >> 16) & 0xFF) / 255,
                  green: Double((v >> 8) & 0xFF) / 255,
                  blue: Double(v & 0xFF) / 255)
    }
    // 테스트 헬퍼
    struct RGB: Equatable { let r, g, b: Double }
    func resolveRGB() -> RGB {
        let ui = UIColor(self); var r: CGFloat = 0, g: CGFloat = 0, b: CGFloat = 0, a: CGFloat = 0
        ui.getRed(&r, green: &g, blue: &b, alpha: &a)
        return RGB(r: (r*255).rounded()/255, g: (g*255).rounded()/255, b: (b*255).rounded()/255)
    }

    static let appBackground = Color(hex: "f4f2ee")
    static let cardSurface = Color.white
    static let primaryText = Color(hex: "15151a")
    static let secondaryText = Color(hex: "8a8a92")
    static let actionBlue = Color(hex: "2D5BFF")
    static let favoriteStar = Color(hex: "FFB300")
}

extension ItemType {
    var accent: Color { Color(hex: accentHex) }
    var gradient: LinearGradient {
        LinearGradient(colors: [Color(hex: gradientHex.0), Color(hex: gradientHex.1)],
                       startPoint: .topLeading, endPoint: .bottomTrailing)
    }
}
```

- [ ] **Step 4: 통과 확인(색)**

Run: 위 동일
Expected: PASS

- [ ] **Step 5: 구현 — TypeTile / FieldRow / Toast (뷰)**

`WadeNote/DesignSystem/TypeTile.swift`:
```swift
import SwiftUI

struct TypeTile: View {
    let type: ItemType
    var size: CGFloat = 38
    var body: some View {
        RoundedRectangle(cornerRadius: size * 0.29, style: .continuous)
            .fill(type.gradient)
            .frame(width: size, height: size)
            .overlay(Image(systemName: type.symbolName)
                .font(.system(size: size * 0.5, weight: .semibold))
                .foregroundStyle(.white))
            .shadow(color: type.accent.opacity(0.5), radius: 6, x: 0, y: 5)
    }
}
```

`WadeNote/DesignSystem/FieldRow.swift`:
```swift
import SwiftUI

struct FieldRow: View {
    let field: Field
    var onCopy: (String) -> Void
    @State private var revealed = false

    private var isSecret: Bool { field.kind == .secret }
    private var display: String {
        guard isSecret, !revealed else { return field.value }
        return String(repeating: "•", count: max(8, min(field.value.count, 12)))
    }

    var body: some View {
        HStack(spacing: 10) {
            VStack(alignment: .leading, spacing: 2) {
                Text(field.label).font(.system(size: 11)).foregroundStyle(Color.secondaryText)
                Text(display)
                    .font(.system(size: 16, design: isSecret ? .monospaced : .default))
                    .foregroundStyle(Color.primaryText)
            }
            Spacer()
            if isSecret {
                Button { revealed.toggle() } label: {
                    Image(systemName: revealed ? "eye.fill" : "eye")
                }.tint(revealed ? Color.actionBlue : Color.secondaryText)
            }
            Button { onCopy(field.value) } label: {
                Image(systemName: "doc.on.doc")
                    .padding(8).background(Circle().fill(Color.actionBlue.opacity(0.1)))
            }.tint(Color.actionBlue)
        }
        .padding(.horizontal, 16).padding(.vertical, 12)
    }
}
```

`WadeNote/DesignSystem/Toast.swift`:
```swift
import SwiftUI

struct ToastModifier: ViewModifier {
    @Binding var message: String?
    func body(content: Content) -> some View {
        content.overlay(alignment: .bottom) {
            if let message {
                Text(message)
                    .font(.system(size: 14, weight: .medium)).foregroundStyle(.white)
                    .padding(.horizontal, 16).padding(.vertical, 10)
                    .background(Capsule().fill(Color.black.opacity(0.85)))
                    .padding(.bottom, 60).transition(.move(edge: .bottom).combined(with: .opacity))
                    .task {
                        try? await Task.sleep(for: .seconds(1.6))
                        withAnimation { self.message = nil }
                    }
            }
        }.animation(.spring(duration: 0.3), value: message)
    }
}
extension View {
    func toast(_ message: Binding<String?>) -> some View { modifier(ToastModifier(message: message)) }
}
```

- [ ] **Step 6: 빌드 확인 + 커밋**

Run: `xcodebuild build -scheme WadeNote -destination 'platform=iOS Simulator,name=iPhone 16'`
Expected: BUILD SUCCEEDED

```bash
git add WadeNote/DesignSystem WadeNoteTests/ColorsTests.swift
git commit -m "feat: add design system tokens, TypeTile, FieldRow, Toast"
```

---

## Task 9: 잠금 화면 + 홈 화면

**Files:**
- Create: `WadeNote/Screens/LockView.swift`, `WadeNote/Screens/HomeView.swift`
- Modify: `WadeNote/App/WadeNoteApp.swift` (환경 객체 주입 필요 시)

**Interfaces:**
- Consumes: `AppLock`, `ItemStore`, `TypeTile`, `Color` 토큰, `Item`.
- Produces:
  - `struct LockView: View { let onUnlock: () async -> Void }`
  - `struct HomeView: View`(모델 컨텍스트에서 `ItemStore` 구성, `@Query`로 항목 표시, 검색·즐겨찾기·유형별 그룹, + 버튼으로 `ItemEditView` 시트, 행 탭으로 `ItemDetailView` push)

- [ ] **Step 1: LockView 구현**

`WadeNote/Screens/LockView.swift`:
```swift
import SwiftUI

struct LockView: View {
    let onUnlock: () async -> Void
    var body: some View {
        ZStack {
            Color.appBackground.ignoresSafeArea()
            VStack(spacing: 22) {
                Spacer()
                RoundedRectangle(cornerRadius: 24, style: .continuous)
                    .fill(LinearGradient(colors: [Color(hex: "3c3c46"), Color(hex: "16161a")],
                                         startPoint: .topLeading, endPoint: .bottomTrailing))
                    .frame(width: 92, height: 92)
                    .overlay(Image(systemName: "lock.doc.fill").font(.system(size: 40)).foregroundStyle(.white))
                    .shadow(color: Color.actionBlue.opacity(0.26), radius: 20, y: 16)
                Text("WadeNote").font(.system(size: 27, weight: .bold))
                Text("잠금됨 · 데이터 비공개").font(.system(size: 13.5)).foregroundStyle(Color.secondaryText)
                Spacer()
                Button { Task { await onUnlock() } } label: {
                    Label("Face ID로 잠금 해제", systemImage: "faceid")
                        .font(.system(size: 16, weight: .semibold))
                }.tint(Color.actionBlue).padding(.bottom, 40)
            }
        }
        .task { await onUnlock() }   // 진입 시 자동 시도
    }
}
```

- [ ] **Step 2: HomeView 구현**

`WadeNote/Screens/HomeView.swift`:
```swift
import SwiftUI
import SwiftData

struct HomeView: View {
    @Environment(\.modelContext) private var context
    @Query(sort: \Item.updatedAt, order: .reverse) private var items: [Item]
    @State private var search = ""
    @State private var showingAdd = false

    private var filtered: [Item] {
        let q = search.trimmingCharacters(in: .whitespaces).lowercased()
        guard !q.isEmpty else { return items }
        return items.filter {
            $0.title.lowercased().contains(q) || $0.type.displayName.lowercased().contains(q)
            || $0.fields.contains { $0.label.lowercased().contains(q) }
        }
    }
    private var favorites: [Item] { filtered.filter(\.isFavorite) }
    private var groups: [(ItemType, [Item])] {
        ItemType.allCases.compactMap { t in
            let xs = filtered.filter { $0.type == t }
            return xs.isEmpty ? nil : (t, xs)
        }
    }

    var body: some View {
        NavigationStack {
            ScrollView {
                if !favorites.isEmpty {
                    sectionHeader("즐겨찾기")
                    ScrollView(.horizontal, showsIndicators: false) {
                        HStack(spacing: 12) {
                            ForEach(favorites) { favCard($0) }
                        }.padding(.horizontal, 22)
                    }
                }
                ForEach(groups, id: \.0) { type, xs in
                    sectionHeader(type.displayName)
                    VStack(spacing: 0) {
                        ForEach(xs) { item in
                            NavigationLink { ItemDetailView(item: item) } label: { row(item) }
                        }
                    }
                    .background(Color.cardSurface, in: RoundedRectangle(cornerRadius: 16))
                    .padding(.horizontal, 22)
                }
            }
            .background(Color.appBackground)
            .navigationTitle("WadeNote")
            .searchable(text: $search, prompt: "검색")
            .toolbar {
                ToolbarItem(placement: .topBarTrailing) {
                    Button { showingAdd = true } label: { Image(systemName: "plus") }
                }
            }
            .sheet(isPresented: $showingAdd) { ItemEditView(mode: .create) }
        }
    }

    private func sectionHeader(_ t: String) -> some View {
        HStack { Text(t.uppercased()).font(.system(size: 11, weight: .semibold))
            .tracking(1.4).foregroundStyle(Color.secondaryText); Spacer() }
            .padding(.horizontal, 24).padding(.top, 18).padding(.bottom, 8)
    }
    private func row(_ item: Item) -> some View {
        HStack(spacing: 13) {
            TypeTile(type: item.type, size: 36)
            VStack(alignment: .leading, spacing: 1) {
                Text(item.title).font(.system(size: 15, weight: .semibold)).foregroundStyle(Color.primaryText)
                if let sub = item.fields.first(where: { !$0.value.isEmpty && $0.kind != .secret })?.value {
                    Text(sub).font(.system(size: 12.5)).foregroundStyle(Color.secondaryText).lineLimit(1)
                }
            }
            Spacer()
            if item.isFavorite { Image(systemName: "star.fill").foregroundStyle(Color.favoriteStar).font(.system(size: 13)) }
        }.padding(.horizontal, 11).padding(.vertical, 9)
    }
    private func favCard(_ item: Item) -> some View {
        VStack(alignment: .leading, spacing: 12) {
            TypeTile(type: item.type, size: 32)
            Text(item.title).font(.system(size: 14, weight: .semibold)).foregroundStyle(Color.primaryText)
        }
        .padding(14).frame(width: 150, alignment: .leading)
        .background(item.type.accent.opacity(0.1), in: RoundedRectangle(cornerRadius: 16))
    }
}
```

- [ ] **Step 3: 빌드 확인**

Run: `xcodebuild build -scheme WadeNote -destination 'platform=iOS Simulator,name=iPhone 16'`
Expected: BUILD SUCCEEDED (`ItemDetailView`/`ItemEditView`는 Task 10에서 — 이 Task는 Task 10과 함께 빌드. 순서상 Task 10 머지 후 빌드 성공.)

- [ ] **Step 4: 수동 검증**

시뮬레이터 실행 → 잠금 화면 표시 → (시뮬레이터 Features > Face ID > Enrolled, Matching Face) → 홈 진입. 빈 상태 확인. + 버튼 동작 확인.

- [ ] **Step 5: 커밋**

```bash
git add WadeNote/Screens/LockView.swift WadeNote/Screens/HomeView.swift
git commit -m "feat: add Lock and Home screens"
```

---

## Task 10: 상세 화면 + 추가/편집 화면 (복사·동기화 마감)

**Files:**
- Create: `WadeNote/Screens/ItemDetailView.swift`, `WadeNote/Screens/ItemEditView.swift`
- Modify: `WadeNote/App/WadeNoteApp.swift` (`SecureClipboard`를 환경에 주입)

**Interfaces:**
- Consumes: `Item`, `Field`, `ItemStore`, `Template`, `FieldRow`, `TypeTile`, `SecureClipboard`, `.toast`.
- Produces:
  - `struct ItemDetailView: View { let item: Item }` — 헤더(타일·제목·유형 뱃지·즐겨찾기 토글) + 필드 카드(FieldRow, 복사 시 토스트) + 첨부 썸네일.
  - `enum EditMode { case create; case edit(Item) }`
  - `struct ItemEditView: View { let mode: EditMode }` — 유형 선택(create) → 템플릿 필드 입력 → 저장.

- [ ] **Step 1: SecureClipboard 환경 주입**

`WadeNote/App/WadeNoteApp.swift`에 추가:
```swift
// body 내 WindowGroup:
WindowGroup { RootView().environment(clipboard) }
    .modelContainer(container)
// 프로퍼티:
@State private var clipboardHolder = ClipboardHolder()
```
`WadeNote/Clipboard/SecureClipboard.swift`에 환경용 래퍼 추가:
```swift
@MainActor @Observable final class ClipboardHolder {
    let clipboard = SecureClipboard()
}
```
(또는 `SecureClipboard`에 `@Observable`를 직접 부여해 `.environment(\.clipboard)`로 주입 — 구현 선택. 아래 상세 화면은 `@Environment(ClipboardHolder.self)` 사용.)

- [ ] **Step 2: ItemDetailView 구현**

`WadeNote/Screens/ItemDetailView.swift`:
```swift
import SwiftUI

struct ItemDetailView: View {
    let item: Item
    @Environment(\.modelContext) private var context
    @Environment(ClipboardHolder.self) private var clip
    @State private var toast: String?

    private var store: ItemStore { ItemStore(context: context) }
    private var sortedFields: [Field] { item.fields.sorted { $0.order < $1.order } }

    var body: some View {
        ScrollView {
            VStack(spacing: 0) {
                TypeTile(type: item.type, size: 72).padding(.top, 10)
                Text(item.title).font(.system(size: 25, weight: .bold)).padding(.top, 14)
                HStack(spacing: 8) {
                    Text(item.type.displayName).font(.system(size: 13, weight: .semibold))
                        .padding(.horizontal, 13).padding(.vertical, 5)
                        .background(item.type.accent.opacity(0.12), in: Capsule())
                        .foregroundStyle(item.type.accent)
                    Button { try? store.toggleFavorite(item) } label: {
                        Image(systemName: item.isFavorite ? "star.fill" : "star")
                            .foregroundStyle(item.isFavorite ? Color.favoriteStar : Color.secondaryText)
                    }
                }.padding(.top, 9)

                VStack(spacing: 0) {
                    ForEach(sortedFields.filter { !$0.value.isEmpty }) { field in
                        FieldRow(field: field) { value in
                            clip.clipboard.copy(value)
                            toast = "복사됨"
                        }
                        Divider().padding(.leading, 16)
                    }
                }
                .background(Color.cardSurface, in: RoundedRectangle(cornerRadius: 16))
                .padding(.horizontal, 22).padding(.top, 20)
            }
        }
        .background(Color.appBackground)
        .navigationTitle("").navigationBarTitleDisplayMode(.inline)
        .toolbar {
            ToolbarItem(placement: .topBarTrailing) {
                NavigationLink("편집") { ItemEditView(mode: .edit(item)) }
            }
        }
        .toast($toast)
    }
}
```

- [ ] **Step 3: ItemEditView 구현**

`WadeNote/Screens/ItemEditView.swift`:
```swift
import SwiftUI

enum EditMode { case create; case edit(Item) }

struct ItemEditView: View {
    let mode: EditMode
    @Environment(\.modelContext) private var context
    @Environment(\.dismiss) private var dismiss

    @State private var type: ItemType = .login
    @State private var title = ""
    @State private var draft: [Field] = []
    @State private var working: Item?

    private var store: ItemStore { ItemStore(context: context) }
    private var isCreate: Bool { if case .create = mode { true } else { false } }

    var body: some View {
        NavigationStack {
            Form {
                if isCreate {
                    Picker("유형", selection: $type) {
                        ForEach(ItemType.allCases) { Text($0.displayName).tag($0) }
                    }.onChange(of: type) { _, t in rebuildDraft(for: t) }
                }
                Section("제목") { TextField("제목", text: $title) }
                Section("필드") {
                    ForEach($draft) { $field in
                        VStack(alignment: .leading) {
                            Text(field.label).font(.caption).foregroundStyle(.secondary)
                            if field.kind == .secret {
                                SecureField(field.label, text: $field.value)
                            } else {
                                TextField(field.label, text: $field.value)
                            }
                        }
                    }
                }
            }
            .navigationTitle(isCreate ? "추가" : "편집")
            .toolbar {
                ToolbarItem(placement: .cancellationAction) { Button("취소") { dismiss() } }
                ToolbarItem(placement: .confirmationAction) { Button("저장") { commit() } }
            }
            .onAppear(perform: load)
        }
    }

    private func load() {
        if case let .edit(item) = mode {
            type = item.type; title = item.title
            draft = item.fields.sorted { $0.order < $1.order }
            working = item
        } else { rebuildDraft(for: type) }
    }
    private func rebuildDraft(for t: ItemType) {
        draft = Template.makeFields(for: t)
    }
    private func commit() {
        if let item = working {                 // 편집
            item.title = title; item.updatedAt = Date()
            try? store.save()
        } else {                                // 신규
            let item = store.create(type: type, title: title)
            for (i, f) in draft.enumerated() {
                item.fields[i].value = f.value
            }
            try? store.save()
        }
        dismiss()
    }
}
```

> 주의: 신규 시 `store.create`가 템플릿 필드를 만들므로 draft 값만 복사. 편집 시에는 기존 `item.fields`를 직접 바인딩(draft가 동일 객체 참조). 값 입력 동작을 수동 검증으로 확인.

- [ ] **Step 4: 빌드 + 회귀 테스트**

Run: `xcodebuild test -scheme WadeNote -destination 'platform=iOS Simulator,name=iPhone 16'`
Expected: BUILD SUCCEEDED, 모든 단위 테스트 PASS.

- [ ] **Step 5: 수동 E2E 검증**

1. 추가 → 로그인 유형 선택 → 제목·아이디·비밀번호 입력 → 저장 → 홈에 표시.
2. 항목 탭 → 상세 → 비밀번호 눈 토글 → 복사 → "복사됨" 토스트.
3. 즐겨찾기 별 → 홈 상단 즐겨찾기 노출.
4. 백그라운드 → 복귀 시 Face ID 재요청.

- [ ] **Step 6: 커밋**

```bash
git add WadeNote/Screens/ItemDetailView.swift WadeNote/Screens/ItemEditView.swift WadeNote/App/WadeNoteApp.swift WadeNote/Clipboard/SecureClipboard.swift
git commit -m "feat: add Detail and Edit screens with copy toast and favorite toggle"
```

---

## Task 11: 사진 첨부 (편집·상세 연동)

**Files:**
- Modify: `WadeNote/Screens/ItemEditView.swift`, `WadeNote/Screens/ItemDetailView.swift`, `WadeNote/App/WadeNoteApp.swift`

**Interfaces:**
- Consumes: `AttachmentStore`, `KeychainKey`, `Item.attachmentIDs`, `PhotosPicker`.
- Produces: 편집 화면에서 사진 선택 → 암호화 저장 → `item.attachmentIDs`에 id 추가; 상세 화면에서 복호화 썸네일 표시.

- [ ] **Step 1: AttachmentStore 환경 구성**

`WadeNoteApp.swift`에 추가:
```swift
static func makeAttachmentStore() throws -> AttachmentStore {
    let key = try KeychainKey.loadOrCreateSymmetricKey(tag: "com.wadenote.attachments.key")
    let dir = URL.documentsDirectory.appendingPathComponent("attachments")
    return AttachmentStore(directory: dir, key: key)
}
```
환경 주입용 홀더:
```swift
@MainActor @Observable final class AttachmentHolder {
    let store: AttachmentStore
    init() { store = try! WadeNoteApp.makeAttachmentStore() }
}
// WindowGroup: RootView().environment(clipboardHolder).environment(AttachmentHolder())
```

- [ ] **Step 2: 편집 화면에 PhotosPicker 추가**

`ItemEditView.swift` Form에 섹션 추가:
```swift
@Environment(AttachmentHolder.self) private var attachments
@State private var pickerItem: PhotosPickerItem?
// ...
Section("사진") {
    PhotosPicker(selection: $pickerItem, matching: .images) { Label("사진 추가", systemImage: "photo") }
        .onChange(of: pickerItem) { _, newItem in
            guard let newItem else { return }
            Task {
                if let data = try? await newItem.loadTransferable(type: Data.self),
                   let id = try? attachments.store.save(data) {
                    working?.attachmentIDs.append(id)
                    try? store.save()
                }
            }
        }
}
```
(상단에 `import PhotosUI`. 신규 항목은 저장 후 working이 생기므로, create 경로에서는 commit 후 첨부를 허용하거나 저장 시 항목을 먼저 생성하도록 흐름 조정 — 수동 검증으로 확인.)

- [ ] **Step 3: 상세 화면에 썸네일 표시**

`ItemDetailView.swift`에 추가:
```swift
@Environment(AttachmentHolder.self) private var attachments
// 필드 카드 아래:
if !item.attachmentIDs.isEmpty {
    LazyVGrid(columns: [GridItem(.adaptive(minimum: 90))], spacing: 10) {
        ForEach(item.attachmentIDs, id: \.self) { id in
            if let data = try? attachments.store.load(id: id), let ui = UIImage(data: data) {
                Image(uiImage: ui).resizable().scaledToFill()
                    .frame(width: 90, height: 90).clipShape(RoundedRectangle(cornerRadius: 12))
            }
        }
    }.padding(.horizontal, 22).padding(.top, 16)
}
```

- [ ] **Step 4: 빌드 + 수동 검증**

Run: `xcodebuild build -scheme WadeNote -destination 'platform=iOS Simulator,name=iPhone 16'`
Expected: BUILD SUCCEEDED
수동: 편집에서 사진 추가 → 상세에서 썸네일 표시. 파일이 `attachments/*.enc`로 암호화 저장됨 확인(평문 미저장).

- [ ] **Step 5: 커밋**

```bash
git add WadeNote/Screens WadeNote/App/WadeNoteApp.swift
git commit -m "feat: add encrypted photo attachments (picker + detail thumbnails)"
```

---

## Task 12: 앱 아이콘 + 보안 마감 (캡처 방지·ADP 안내)

**Files:**
- Create: `WadeNote/Assets.xcassets/AppIcon.appiconset/*` (라이트/다크 1024 마스터)
- Modify: `WadeNote/App/RootView.swift` (잠금 중 스냅샷 가림), `WadeNote/Screens/HomeView.swift` (ADP 안내 1회)

**Interfaces:**
- Consumes: `AppLock`.
- Produces: 적응형 앱 아이콘, 백그라운드 시 가림 오버레이, 최초 1회 iCloud 고급 데이터 보호(ADP) 안내 배너.

- [ ] **Step 1: 앱 아이콘 자산 생성**

`docs/superpowers/specs/2026-06-22-wadenote-visual-design.md` §6의 SVG 글리프로 1024×1024 PNG 2종(라이트 배경 `#eceef3→#d7dbe5`, 다크 배경 `#2c2c34→#0e0e11`) 렌더 후 `AppIcon.appiconset`에 등록.
```bash
# rsvg-convert 또는 디자이너 도구로 1024 렌더. 예:
# rsvg-convert -w 1024 -h 1024 wadenote-icon-light.svg -o icon-1024-light.png
```
Asset Catalog에서 "Single Size" + Appearances: Any/Dark 지정(iOS 18 적응형 아이콘).

- [ ] **Step 2: 잠금 중 캡처/스냅샷 가림**

`RootView.swift` 수정 — 백그라운드 진입 시 가림 오버레이:
```swift
.overlay {
    if scenePhase != .active {
        ZStack { Color.appBackground.ignoresSafeArea()
            Image(systemName: "lock.fill").font(.largeTitle).foregroundStyle(Color.secondaryText) }
    }
}
```

- [ ] **Step 3: ADP 안내 배너(최초 1회)**

`HomeView.swift`에 `@AppStorage("didShowADPNotice") var didShowADPNotice = false` 추가, 첫 진입 시 상단에 안내:
```swift
if !didShowADPNotice {
    Button { didShowADPNotice = true } label: {
        Label("설정 > Apple 계정 > iCloud > 고급 데이터 보호를 켜면 데이터가 종단간 암호화됩니다. (확인)",
              systemImage: "checkmark.shield")
    }.font(.footnote).padding(.horizontal, 22)
}
```

- [ ] **Step 4: 빌드 + 수동 검증**

Run: `xcodebuild build -scheme WadeNote -destination 'platform=iOS Simulator,name=iPhone 16'`
Expected: BUILD SUCCEEDED
수동: 홈 화면 아이콘(라이트/다크) 확인, 앱 스위처 미리보기 가림 확인, ADP 안내 1회 표시 후 사라짐.

- [ ] **Step 5: 커밋**

```bash
git add WadeNote/Assets.xcassets WadeNote/App/RootView.swift WadeNote/Screens/HomeView.swift
git commit -m "feat: add adaptive app icon, snapshot masking, ADP notice"
```

---

## Self-Review

**Spec coverage (설계+비주얼 문서 → 태스크 매핑):**
- 유형 4종 + 사용자정의 필드 → Task 2,3,10 ✓
- Face ID 잠금 + 자동 재잠금 → Task 7,12 ✓
- CloudKit 동기화/복원 + ADP 안내 → Task 1,2(모델 CloudKit-safe),12 ✓
- 필드 빠른 복사(자동삭제) → Task 6,10 ✓
- 검색(라벨만)/즐겨찾기 → Task 4,9 ✓
- 사진 첨부(암호화) → Task 5,11 ✓
- 비주얼: 색 토큰·그라데이션 타일·필드 행·토스트·4화면·앱 아이콘 → Task 8,9,10,12 ✓

**Placeholder scan:** 모든 코드 단계에 실제 코드 포함. "적절한 에러 처리" 류 모호 표현 없음. (Task 11 create-경로 첨부 흐름은 수동 검증 명시.)

**Type consistency:** `ItemStore.create/save/toggleFavorite/all/search/favorites`, `Template.fields/makeFields`, `AttachmentStore.save/load/delete`, `SecureClipboard.copy/clearNow`, `AppLock.unlock/lock`, `ItemType.accentHex/gradientHex/symbolName/accent/gradient` — 정의 태스크와 사용 태스크 간 시그니처 일치 확인.

**알려진 빌드 순서 의존:** RootView(Task 7)·HomeView(Task 9)는 Detail/Edit(Task 10) 존재 후 컴파일 성공. 실행은 Task 1→2→…→12 순서이되, 화면 4개(8~11)는 한 묶음으로 빌드 검증. subagent-driven 실행 시 이 의존을 리뷰 체크포인트로 둔다.
